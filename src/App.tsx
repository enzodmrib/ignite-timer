import { Button } from "./components/Button";

export function App() {
  return (
    <>
      <Button variant={'primary'} />
      <Button variant={'primary'} />
      <Button variant={'primary'} />
      <Button variant={'primary'} />
      <Button variant={'primary'} />
    </>
  )
}

